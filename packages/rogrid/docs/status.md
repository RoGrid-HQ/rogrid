# Status

RoGrid is not ready for use yet.

This version exists only to test the `rogrid` CLI end to end against a real registry. The library itself is being worked on next, and its API will change without notice until a stable release is announced.
